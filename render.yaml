services:
  - type: web
    name: ludo_game
    env: node
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /health
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
